{
  "name": "employee_tracker",
  "version": "1.0.0",
  "description": "To install dependencies:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node src/index.ts",
    "db:setup": "psql -U $DB_USER -d $DB_NAME -f ./sql/schema.sql && psql -U $DB_USER -d $DB_NAME -f ./sql/seed.sql",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console.table": "^0.10.0",
    "dotenv": "^16.4.7",
    "inquirer": "^12.1.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/node": "^22.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
